---
title: "Fluid Type Scales"
abstract: "Using CSS `clamp` and custom properties, we can create responsive type scales."
publishedOn: "2023-11-13T12:00:00-0800"
tags: ["css"]
---

Using CSS `clamp` and custom properties, we can create responsive type scales.

We can use a generator like [this](https://www.fluid-type-scale.com/) to output custom properties to drop into a design system.
